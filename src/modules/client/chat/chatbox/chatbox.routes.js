const chatboxController = require("./chatbox.controller");

const router = require("express").Router();

// router.get("/chat",chatboxController.renderChatRoom)
module.exports = {
    ChatboxRoutes:router
}